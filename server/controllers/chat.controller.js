// TODO: Обработчик req/res. Извлеки message из req.body, вызови chatService.sendMessage(message).
// Верни res.status(200).json({ reply }). При ошибке — вызови next(err) или обработай через errorHandler.
